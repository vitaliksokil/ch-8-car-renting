import { NetworkId } from "./Options";
export interface NetworkConfiguration {
    networkId: string;
    nodeUrl: string;
    helperUrl: string;
    explorerUrl: string;
    restApiUrl: string;
}
export declare const getNetwork: (networkId: NetworkId) => NetworkConfiguration;
export declare const resolveNetwork: (network: NetworkId | NetworkConfiguration) => NetworkConfiguration;
